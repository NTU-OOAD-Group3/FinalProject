import java.util.*;
import java.io.*;
import org.json.*;
import java.time.*;
import java.time.format.DateTimeFormatter;
import java.time.temporal.ChronoUnit;

import org.apache.commons.io.IOUtils;
import org.apache.commons.io.FileUtils;
import org.apache.commons.io.IOExceptionWithCause;

import com.google.gson.*;

import java.text.DateFormat;
import java.text.ParseException;
import java.text.SimpleDateFormat;
public class main {
    public static void main(String[] args) {
        //via DB:
       
        //LoginStatus status =new LoginStatus();//MODEL
        //LoginControl loginControl = new LoginControl(status);//CONTROL with MODEL
        //MainFrame mainFrame = new MainFrame(loginControl,xxxControl,...);//VIEW with CONTROL
       // loginControl.setView(mainFrame.getLoginUI());//ADD VIEW INTO CONTROL
    }
}